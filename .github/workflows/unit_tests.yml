name: Unit Tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: cd DraftCleaner
    - run: npm install
    - run: npm test
    - run: cd ../GANReviewTool
    - run: npm install
    - run: npm test
    - run: cd ../NotSoFast
    - run: npm install
    - run: npm test
    - run: cd ../SpeciesHelper
    - run: npm install
    - run: npm test
    - run: cd ../UserRightsDiff
    - run: npm install
    - run: npm test
